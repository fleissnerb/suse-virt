defaultNamespace: rook-ceph
dependsOn:
  - selector:
      fleet.cattle.io/bundle-directory: rook-operator
helm:
  repo: https://charts.rook.io/release
  chart: rook-ceph-cluster
  version: "v1.19.2"
  valuesFiles:
    - rook-cluster-values.yaml
